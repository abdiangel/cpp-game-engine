{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with CodeLLDB",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/bin/game_engine",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "make all",
      "stopOnEntry": false
    }
  ]
}